-- creates the table force_name. the db will be passed as argument to mysql
-- table schema = id INT, name VARCHAR(256) NOT NULL
CREATE TABLE IF NOT EXISTS force_name(id INT, name VARCHAR(256) NOT NULL);
