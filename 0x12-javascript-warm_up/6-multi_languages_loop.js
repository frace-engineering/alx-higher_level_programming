#!/usr/bin/node
/* print 3 lines using array of strings and a loop */
const array_of_str = ['C is fun', 'Python is cool', 'Javascript is amazing'];
for (let i = 0; i < array_of_str.length; i++) {
  console.log(array_of_str[i]);
}
